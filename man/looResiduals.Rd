% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generic.R, R/looResiduals.R
\name{looResiduals}
\alias{looResiduals}
\alias{looResiduals,FLSR-method}
\alias{looResiduals,FLStock-method}
\title{Leave-one-out residuals}
\usage{
looResiduals(object, ...)

\S4method{looResiduals}{FLSR}(object, log = TRUE, years = NULL, quiet = TRUE, ...)

\S4method{looResiduals}{FLStock}(
  object,
  model = "geomean",
  log = TRUE,
  years = NULL,
  quiet = TRUE,
  ...
)
}
\arguments{
\item{object}{An \code{FLSR} or \code{FLStock}.}

\item{...}{Passed to methods.}

\item{log}{Logical; if \code{TRUE} (default), residuals are
\code{log(rec) - log(hat)}, matching \code{FLSR} with \code{logerror=TRUE}.}

\item{years}{Optional character or numeric years to leave out. Default is
all years with finite \code{rec} and \code{ssb}.}

\item{quiet}{Suppress \code{fmle} console output.}

\item{model}{SR model passed to \code{as.FLSR} (default \code{"geomean"}).}
}
\value{
An \code{FLQuant} of LOO residuals (same dims as \code{rec(object)}).
Years that fail to refit are \code{NA}.
}
\description{
Generic for leave-one-out (LOO) residuals. For an \code{FLSR}, each year is
dropped in turn, the stock–recruit model is re-fitted, and the residual for
the held-out year is computed from the predicted recruitment at that year's
SSB.
}
\examples{
\dontrun{
data(ple4)
sr <- fmle(as.FLSR(ple4, model = "geomean"))
loo <- looResiduals(sr)
plot(loo)
}

}
